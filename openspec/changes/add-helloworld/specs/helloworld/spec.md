# Specification — Hello World Page

## Requirement
The site SHALL provide a `helloworld.html` page that renders the text "Hello World".

## Scenario
- Given the repository root
- When a visitor opens `helloworld.html` in a browser
- Then the page displays "Hello World" as visible page content
- And the page uses UTF-8 encoding and declares `lang="zh-CN"`
